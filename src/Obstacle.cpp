#include "Cell.hpp"
#include "Obstacle.hpp"


Obstacle::Obstacle(int healthPoints, int destroyFrame, std::vector<Cell*> cover) : healthPoints(healthPoints), destroyFrame(destroyFrame), cover(cover)
{
	/* TODO*/
}
